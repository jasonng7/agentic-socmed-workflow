# Unified Socmed Scraper
# VPS Setup:
#   1. python3 -m venv .venv && source .venv/bin/activate
#   2. pip install -r requirements.txt
#   3. sudo apt install ffmpeg
#   4. streamlit run app.py --server.port 8501 --server.address 0.0.0.0

streamlit>=1.30.0
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.0
openpyxl>=3.1.0
instaloader>=4.10
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# Install ONE Whisper backend:
# macOS (Apple Silicon): pip install mlx-whisper
# Linux (VPS):           pip install openai-whisper
